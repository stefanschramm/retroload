{
  "name": "retroload",
  "workspaces": [
    "retroload-basic",
    "retroload-common",
    "retroload-cli",
    "retroload-encoders",
    "retroload-examples"
  ],
  "scripts": {
    "build": "tsc --build -v",
    "build-watch": "tsc --build -v --watch"
  }
}
