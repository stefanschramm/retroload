{
  "name": "retroload",
  "workspaces": [
    "retroload-basic",
    "retroload-common",
    "retroload-cli",
    "retroload-encoders",
    "retroload-examples"
  ],
  "scripts": {
    "build": "tsc --build -v",
    "clean": "tsc --build --clean",
    "build-watch": "tsc --build -v --watch"
  }
}
